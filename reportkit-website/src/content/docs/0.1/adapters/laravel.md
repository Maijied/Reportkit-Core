---
title: "Laravel 5.5–13"
description: "Modern Illuminate adapter."
order: 10
group: "Adapters"
---

Package: `reportkit/laravel`

- Auto-discovered service provider + `ReportKit` facade
- Blade CAS pack (`reportkit::ui.*`)
- `reportkit:install` / `reportkit:make`
- Opt-in routes: `/reportkit/{slug}/weeks`, `/rows`, `/data`, `/trace`
- `ReportKit::merged()` + `ReportKit::connection()` for dual-DB
