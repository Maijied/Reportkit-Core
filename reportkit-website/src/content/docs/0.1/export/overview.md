---
title: "CSV & filenames"
description: "Export helpers."
order: 60
group: "Export"
---

`ExportHelper` sanitizes filename parts and builds download names. Optional `CsvWriter` streams CSV from prepared rows. Excel/PDF remain host recipes.
