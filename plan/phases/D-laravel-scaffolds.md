# Phase D — Laravel adapter

**Doc:** [plan index](../README.md)

---

| Task | Deliverable |
|------|-------------|
| D1 Preset `hybrid-export` | weeks + data + send + full Blade stack |
| D2 Preset `datatables-sync` | browse-only reports |
| D3 Preset `hybrid-kpi` | prepare + KPI grid |
| D4 `reportkit:install` | config, views, assets, email template |
| D5 `reportkit:make` stubs | match PR file layout exactly |
| D6 `@include` composer | auto-register partial paths + sections |

Generated page must `@extend('reportkit::layouts.report')` and fill sections — **not** copy 4,000 lines inline.

---
